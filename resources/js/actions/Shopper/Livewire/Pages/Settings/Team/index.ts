import Index from './Index'
import RolePermission from './RolePermission'

const Team = {
    Index: Object.assign(Index, Index),
    RolePermission: Object.assign(RolePermission, RolePermission),
}

export default Team